{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "env": {
    "NEXT_PUBLIC_TARGET_CITY": "PENHA",
    "NEXT_PUBLIC_TARGET_STATE": "SC"
  },
  "functions": {
    "src/app/api/**": {
      "maxDuration": 30
    }
  }
}
